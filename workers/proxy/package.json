{
  "name": "npx-proxy",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker — proxies Firecrawl and Anthropic API calls with rate limiting",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260401.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.0"
  }
}
