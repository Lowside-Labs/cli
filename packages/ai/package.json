{
  "name": "@npx/ai",
  "version": "0.0.0",
  "private": true,
  "description": "Typed AI client — structured generation via proxy to Anthropic API",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "tsdown": "^0.21.7",
    "typescript": "^5.8.3"
  }
}
